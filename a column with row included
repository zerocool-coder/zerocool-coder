import 'dart:ui';

import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}
class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        appBar: AppBar(
          backgroundColor: Colors.brown,
          title: Center(child:Text('zEROcool'),),
        ),
      body: ListView(
      padding: const EdgeInsets.all(20),
            children: [
              Container(
                height: 50,
                color: Colors.yellow,
                child: const Center(child: Text('pentesting'),),
              ),
              Container(
                height: 50,
                color: Colors.white,
                child: const Center(child: Text('Forensics'),),
              ),
              Container(
                height: 50,
                color: Colors.deepPurpleAccent,
                child: const Center(child: Text('Wireshark '),),
              ),
              Container(
                height: 50,
                color: Colors.white,
                child: const Center(child: Text('qemu'),),
              ),
              Container(
                height: 50,
                color: Colors.yellow,
                child: const Center(child: Text('u-boot '),),
              ),
              Container(
                height: 50,
                color: Colors.white,
                child: const Center(child: Text('otrace'),),
              ),
              Container(
                height: 50,
                color: Colors.yellow,
                child: const Center(child: Text('abootimg '),),
              ),
              Container(
                height: 50,
                color: Colors.white,
                child: const Center(child: Text('aeskeyfind'),),
              ),
              Container(
                height: 50,
                color: Colors.deepPurpleAccent,
                child: const Center(child: Text('aesfix'),),
              ),
              Container(
                height: 50,
                color: Colors.white,
                child: const Center(child: Text('afflib'),),
              ),
              Container(
                height: 50,
                color: Colors.yellow,
                child: const Center(child: Text('gdb-minimal '),),
              ),
              Container(
                height: 50,
                color: Colors.white,
                child: const Center(child: Text('gdisk'),),
              ),
              Container(
                height: 50,
                color: Colors.yellow,
                child: const Center(child: Text('udptunnel'),),
              ),
              Container(
                child: Row(
                  mainAxisSize: MainAxisSize.max,
                  textDirection: TextDirection.ltr,
                  children: <Widget>[
                    const FlutterLogo(),
                    const Expanded(
                      child: Text("More than 600 penetration testing tools included"),
                    ),
                    const Icon(Icons.view_in_ar),
                  ],
                )

                )
            ],
            scrollDirection: Axis.vertical,
          )
              ),
            );
}
}


